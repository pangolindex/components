import React from 'react';
import { SwapWidget } from '@components/index';
import { SwapWidgetWrapper } from './styled';

function Swap() {
  return (
    <SwapWidgetWrapper>
      <SwapWidget />
    </SwapWidgetWrapper>
  );
}

export default Swap;
